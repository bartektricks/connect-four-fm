import BoardBackground from "components/board-background/BoardBackground";
import RulesBlock from "components/rules-block/RulesBlock";

const Rules = () => {
  return (
    <BoardBackground backgroundColor="purple">
      <RulesBlock />
    </BoardBackground>
  );
};

export default Rules;
